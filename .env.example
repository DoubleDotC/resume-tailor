# Optional environment variables. The default pipeline runs entirely on local
# MLX models and does NOT require any of these. Copy to .env only if you wire
# in an external provider yourself.

# OPENAI_API_KEY=sk-...
